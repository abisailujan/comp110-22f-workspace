"""Some helpful utility functions for working in CSV files."""


def read_csv_rows(filename: str) -> list[dict[str, str]]:
    """Read the rows of a CSV into a 'table' aka list of disctionaries."""
    result: list[dict[str, str]] = []
    # TODO: More work
    return result